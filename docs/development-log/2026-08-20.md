# Development record - 2026-08-20

## Scope and authority

This development slice prepared the review-ready daily 24-hour recursive
stability decision memo requested by the maintainer. It changed documentation
only. No core production file, contract, active invariant, deterministic gate,
model, forecast, approval, or external system was changed.

## Evidence reviewed

- Active leakage-safe chronological invariant `TAXI-MODEL-001`.
- Existing deterministic release policy and the recursive-shadow implementation
  and matched tests.
- The durable candidate-v2 summary in this log's 2026-08-09 predecessor.
- The ignored local v1 and v2 recursive-shadow reports, including their report
  and model digests, daily records, drift-check results, and 24 horizon segments.

The v2 evidence covers 2026-05-01 through 2026-05-31. It shows 12.32% combined
WAPE improvement over the previous-week baseline, 18/31 daily wins, 29.79%
worst-day relative degradation, and 21/31 daily drift passes. All 24 aggregated
horizon hours beat the baseline, but horizon hour is identical to UTC clock hour
because each forecast begins at midnight.

## Decision memo

[`../recursive-stability-decision-memo-2026-08-20.md`](../recursive-stability-decision-memo-2026-08-20.md)
recommends these evidence roles:

- daily win rate: proposed release criterion;
- worst-day relative degradation: proposed release criterion;
- daily drift pass rate: advisory release evidence; and
- current horizon profile: diagnostic-only.

The memo deliberately does not select numeric thresholds from the one-month
sample. Candidate v2 remains `HOLD`, and human review is required before any
release-gate implementation or new candidate evaluation.

## Validation

- Codex-run validation: `.venv\Scripts\python.exe -m pytest -q
  tests/test_recursive_shadow_evaluation.py tests/test_forecast.py
  tests/test_quality_gates.py` returned `14 passed` with one existing
  joblib/loky physical-core detection warning.
- Codex-run validation: `.venv\Scripts\python.exe -m pytest -q` returned
  `88 passed` with the same warning.
- Codex-run validation: the memo's wins, losses, drift passes, worst-day result,
  and horizon wins were recomputed from the v2 JSON and matched the recorded
  values; `git diff --check` passed for tracked changes.
- Incomplete environment attempt: the system Anaconda `python -m pytest`
  invocation stopped before test collection because its `_ssl` DLL could not be
  loaded. The repository virtual environment was then used successfully.
- Incomplete validation: the AgentGov repository check was not rerun because
  the `agentgov` module is absent from both available Python environments. This
  slice does not change governance or agent protocol; the 2026-08-14 governance
  result remains historical evidence only.
- User-reported validation: none.
- Pending validation: maintainer review of each proposed metric role and the
  threshold-design method.

## Remaining

- Record the maintainer's `accept`, `revise`, or `reject` disposition for the
  memo; do not interpret this document as approval.
- After review, produce a bounded attribution packet for the 13 losing days
  using only pre-forecast information.
- Separate horizon from clock hour before proposing a horizon stability gate.
- Preserve the separate model-package identity, runner, governance, fare, and
  Sydney work listed in the canonical current status.

No commit, push, promotion, publication, deployment, or scheduling action was
performed or authorized by this slice.

## Maintainer review and follow-on loss-day attribution

After the initial memo closeout, the maintainer accepted all four proposed
metric roles. The memo now records that decision while preserving the separate
boundary for numeric thresholds, core-file changes, training, promotion, and
publication.

The authorized follow-on diagnosis produced
[`../recursive-loss-day-attribution-2026-08-20.md`](../recursive-loss-day-attribution-2026-08-20.md).
It used deterministic calendar fields and demand strictly earlier than each
forecast day for explanatory comparisons. Target-day actuals were used only to
reproduce the win/loss label and localize error after the forecast.

The main descriptive findings were:

- all 13 losses occurred on or before May 20, followed by 11 consecutive wins;
- 11 losses were ordinary weekdays, two were ordinary weekend days, and the two
  Memorial Day event-window days both won;
- airport predictions reduced absolute error relative to the baseline across
  the losing days, while non-airport predictions accounted for all net excess;
- excess error was concentrated in high-volume Manhattan zones on the global
  path; and
- 12/13 losses passed absolute drift checks, while 9/18 wins failed at least one
  drift check.

These are observational localizations and hypotheses, not causal findings or
new release rules.

### Follow-on validation

- Codex-run validation: candidate v2 was replayed observationally across 82,056
  losing-day zone-hour rows; all 13 days reproduced as candidate losses.
- Codex-run validation: all 13 pre-forecast daily table rows were recomputed
  from the governed hourly SHA and matched the report exactly; the drift
  cross-tab reproduced one failing loss and nine failing wins.
- Codex-run validation: the complete repository suite returned `88 passed` with
  the existing joblib/loky physical-core detection warning.
- Codex-run validation: relative documentation links and `git diff --check`
  passed.
- User-reported validation: the maintainer accepted the four metric roles; no
  user validation of the attribution findings has been reported.
- Pending validation: test the descriptive Manhattan/global-model and recent
  demand hypotheses on pre-registered, disjoint out-of-time periods.

No core production file or contract was modified, and no production operation
or external mutation was performed.

## Session closeout and delivery authorization

The maintainer stated that the day's work was complete and explicitly
authorized committing the recursive-stability documentation and normally
pushing it to the configured GitHub `main` branch.

The authorized first delivery scope is limited to:

- `docs/recursive-stability-decision-memo-2026-08-20.md`;
- `docs/recursive-loss-day-attribution-2026-08-20.md`;
- `docs/current-development-status.md`; and
- `docs/development-log/2026-08-20.md`.

Existing untracked AgentGov, governance-task, 2026-08-13 log, and showcase
files remain outside this delivery. The actual commit and push results are to
be appended after the Git operations complete.

## Delivery result

- Feature and evidence commit: `18ac9f8` (`Document recursive stability and
  loss attribution`).
- Push result: normal non-force push to configured `origin/main` succeeded;
  remote `main` advanced from `6e1e1ca` to `18ac9f8`.
- Delivered files matched the four-file authorized scope above.
- Existing unrelated untracked files remained uncommitted and untouched.

This delivery did not authorize or perform model promotion, forecast
publication, deployment, scheduling, repository-setting changes, or another
external mutation.
