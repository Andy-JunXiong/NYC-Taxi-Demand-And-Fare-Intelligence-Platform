# NYC Taxi development record — 2026-08-05

## Outcome

The non-core portion of the approval-gate P0 slice is complete. Approval
records now have an inert, checked template and field guidance. Model promotion
integration coverage proves the waiting, approved, checksum-mismatch, and
failed-model-gate paths, while approval parsing and copy failures preserve the
prior production artifact and remove partial output.

## Development completed

- Added a safe-by-default approval template with `approved: false` and explicit
  placeholders for action, reviewer, timestamp, and artifact SHA-256.
- Documented separate approval records for `model_promotion` and
  `forecast_publication`, including the no-secrets and no-production-authority
  boundaries.
- Added `rolling_backtest` integration tests for awaiting approval, exact
  approved promotion, checksum mismatch, and a failed model gate.
- Converted malformed or unreadable approval JSON into a bounded approval
  rejection.
- Removed temporary promotion output after an interrupted copy while preserving
  the existing production artifact.

## Remaining P0 decisions

- Name the authoritative retention location and policy for original approval
  JSON files outside Git. Existing run-ledger and forecast-lineage records retain
  attributable approval metadata, but not the original approval file.
- Rehearse self-hosted Windows workflow dispatch without publishing a forecast.
- Obtain specific approval before modifying any core file if either remaining
  item requires it.

## Validation checkpoint

- Targeted approval and model-validation tests: 14 passed.
- NYC project suite: 50 passed under Python 3.12 using the existing repository
  dependencies and a workspace-local pytest temporary directory.
- Development-source AgentGov repository check:
  `PASS=17 WARN=1 FAIL=0 ADVISORY=4`.
- AgentGov agent-skill contracts: `PASS=4 FAIL=0`.
- `git diff --check`: passed before this record was added and is rerun at
  handoff.

The bare repository-root pytest invocation also collected the pre-existing,
untracked nested `agent-governance-starter/` test suite. That combined run was
not an NYC project-suite result and failed because the nested suite resolved an
installed AgentGov package without its distribution assets. The nested source
directory was left untouched and excluded from this change.

## Not performed

No production workflow was dispatched, approval was granted, model was
promoted, forecast was published, external system was changed, or core file was
modified.
